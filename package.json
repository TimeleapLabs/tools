{
  "name": "timeleap-tools",
  "module": "index.ts",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.8.3"
  },
  "private": true,
  "scripts": {
    "build": "bun --filter '*' build",
    "build:all": "bun --filter '*' build:all"
  },
  "type": "module",
  "workspaces": [
    "apps/**",
    "packages/**"
  ]
}

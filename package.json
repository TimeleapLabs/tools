{
  "name": "timeleap-tools",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun --filter '*' build",
    "build:all": "bun --filter '*' build:all"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "apps/**",
    "packages/**"
  ]
}
